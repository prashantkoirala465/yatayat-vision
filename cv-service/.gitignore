.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/

# local datasets, trained/exported model weights - never committed, regenerate
# from the training notebook (see docs/models/) rather than tracking in git
data/*
!data/README.md
runs/

# ultralytics auto-downloads pretrained checkpoints into the cwd if not told
# otherwise - catches those wherever they land, not just under data/
*.pt
*.onnx
*.mlpackage/
