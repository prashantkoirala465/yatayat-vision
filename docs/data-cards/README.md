# Data cards

One short file per dataset used in this project, added as each one actually gets used (not upfront): what it is, its license, what subset was taken and why, and any known limitation that affects results downstream. The goal is that anyone reading the repo can tell exactly what the models did and didn't see during training, without digging through notebooks.
